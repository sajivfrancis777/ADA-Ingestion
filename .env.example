# GitHub Personal Access Token (fine-grained, read-only on IAO-Architecture repo)
# Without this: 60 req/hr (anonymous). With this: 5,000 req/hr.
# Create at: https://github.com/settings/tokens?type=beta
# Scope: sajivfrancis777/IAO-Architecture → Contents: Read-only
VITE_GITHUB_TOKEN=
